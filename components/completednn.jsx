import { StyleSheet, Text, View } from "react-native";
import React from "react";

const completednn = () => {
  return (
    <View>
      <Text>completednn</Text>
    </View>
  );
};

export default completednn;

const styles = StyleSheet.create({});
